<UserControl x:Class="ZipGameImporter.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZipGameImporter" >
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Text="ZIP source folder" Grid.Row="0" Grid.Column="0" Margin="5"/>

        <TextBox Text="{Binding Settings.IncomingFolder}" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <Button Content="Browse" Grid.Row="0" Grid.Column="2"
                    Command="{Binding BrowseIncomingCommand}"
                    Margin="5"/>


        <TextBlock Text="Games installation folder" Grid.Row="1" Grid.Column="0"
                   Margin="5"/>

        <TextBox Text="{Binding Settings.GamesFolder}" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <Button Content="Browse" Grid.Row="1" Grid.Column="2"
                    Command="{Binding BrowseGamesCommand}"
                    Margin="5"/>

    </Grid>
</UserControl>
