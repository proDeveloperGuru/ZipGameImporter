<UserControl x:Class="ZipGameImporter.Controls.ImportDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZipGameImporter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="10" Grid.Row="0"
                Grid.Column="0">
            <Button Content="Add Import"
                Command="{Binding AddImportCommand}"/>
        </StackPanel>


        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="0"
                Grid.Column="1">
            <ItemsControl ItemsSource="{Binding Settings}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10"
                            Padding="10"
                            BorderBrush="Gray"
                            BorderThickness="1">
                            <local:SettingsView/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.ColumnSpan="2" Grid.Row="1"  Orientation="Horizontal" HorizontalAlignment="Right"
                Grid.Column="0">
            <Button Content="Atcelt" Margin="10"
                Command="{Binding CancelCommand}"/>
            <Button Content="Importēt" Margin="10"
                Command="{Binding ImportCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
